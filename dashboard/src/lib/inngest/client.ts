import { Inngest } from "inngest";

// Generate types based on your event definitions
// We will define 'collection.start' when we create the function
export const inngest = new Inngest({ id: "clover-dashboard" });
